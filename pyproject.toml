[project]
name = "agent_workforce"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anas Seth", email = "m.anasseth00@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "docx>=0.2.4",
    "openai-agents>=0.2.8",
    "pandas>=2.3.1",
    "pypdf>=6.0.0",
    "python-docx>=1.2.0",
    "requests>=2.32.4",
]

[project.scripts]
start-agent = "agent_workforce.main:start"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
